# Two extensions developed in Typescript for Directus 

- coords_to_map - Generates a map from coordenates (Readme inside folder)
- coords_to_text - Generates a address from coordenates (Readme inside folder)
